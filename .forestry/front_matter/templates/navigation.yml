---
label: Navigation
hide_body: true
fields:
- name: navigation_title
  type: text
  config:
    required: false
  label: Navigation Title
  description: Use this field when using an external field or when you need to use
    an alternative title other than the page title (eg. page title is too long)
- name: select_page
  type: select
  default: []
  config:
    required: false
    options: []
    source:
      type: pages
      section: pages-dir
      file: 
      path: 
  label: Select Page
  showOnly:
    field: external_link
- name: external_link
  type: boolean
  label: External Link?
- name: url
  type: text
  config:
    required: true
  label: Url
  showOnly:
    field: external_link
    value: true
pages:
- content/options/navigation.json
